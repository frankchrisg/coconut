package com.template.contracts;

import net.corda.core.contracts.CommandData;
import net.corda.core.contracts.Contract;
import net.corda.core.transactions.LedgerTransaction;
import org.jetbrains.annotations.NotNull;

public class KeyValueContract implements Contract {

    public static class Get implements CommandData {
    }

    public static class Set implements CommandData {
    }

    @Override
    public void verify(@NotNull final LedgerTransaction tx) {
    }

    public static final class KeyValueGetContract implements Contract {
        public static class Get implements CommandData {
        }

        @Override
        public void verify(@NotNull final LedgerTransaction tx) {
        }
    }
}