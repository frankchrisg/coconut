package com.template.contracts;

import net.corda.core.contracts.CommandData;
import net.corda.core.contracts.Contract;
import net.corda.core.transactions.LedgerTransaction;
import org.jetbrains.annotations.NotNull;

public class IOExtendedContract implements Contract {

    public static class Write implements CommandData {
    }

    public static class Scan implements CommandData {
    }

    public static class RevertScan implements CommandData {
    }

    @Override
    public void verify(@NotNull final LedgerTransaction tx) {
    }
}