- name: Check if operating system is Ubuntu
  fail: msg="Please use Ubuntu as operating system."
  when: ansible_distribution != 'Ubuntu'
- name: Restart Docker daemon
  systemd:
    state: restarted
    daemon_reload: yes
    name: docker
  become: true
- name: Build an Ubuntu image for graphene
  docker_image:
    build:
      network: host
      path: "{{ DOCKERFILE_PATH }}/"
      nocache: "yes"
      pull: "yes"
    name: ubuntu-graphene
    force_source: "{{ REBUILD_UBUNTU_GRAPHENE_DOCKERFILE }}"
    source: "build"
